import React, { Component } from "react";
import styles from '../main.module.css'

class Button extends Component {
    render() {
        return (
            <button className={styles.button1}> Category</button>
        )
    }
}

export default Button